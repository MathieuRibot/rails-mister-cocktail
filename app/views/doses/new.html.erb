<div class="container text-center">
  <h1>Add a new dose to your <%= @cocktail.name %></h1>
</div>

<div class="container flex-align-justify-center">
  <div class="form-container">
    <%= render 'form', dose: @dose %>
  </div>
</div>

<div class="footer">
  <%= link_to "Back to #{@cocktail.name}", cocktails_path(@cocktail) %>

  <%= link_to 'Back to Cocktails List', cocktails_path %>
</div>
